for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    ans = max(a)
    for i in range(n):
        for j in range(i + 1, n):
            if a[i] <= a[j]:
                ans = max(ans, a[i] + a[j])          
    print(ans)
