t = int(input())
for _ in range(t):
    n = int(input())
    # Read the temperatures into a list
    a = list(map(int, input().split()))
    
    mn = min(a)
    mx = max(a)
   
    count = 0
    for x in a:
        if x != mn and x != mx:
            count += 1
            
    print(count)
