a = list(map(int, input().split()))
key = int(input())

found = False
for i in range(len(a)):
    if a[i] == key:
        print("Found at", i)
        found = True
        break
if not found:
    print("Not Found")
